# Base URL of the backend API (includes /api/v1)
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Client-side gate password for the admin dashboard (NOT real auth - the
# backend has no authentication system; this only hides the /admin UI).
VITE_ADMIN_PASSWORD=admin123
